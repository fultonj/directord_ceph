controller:
  services:
    - 2node_config
    - ceph_make_spec
    - ssh_user
    - repos
    - ceph_packages
    - ceph_bootstrap
    - ceph_apply_spec

ceph-storage:
  services:
    - 2node_config
    - ssh_user
    - repos
    - ceph_packages
